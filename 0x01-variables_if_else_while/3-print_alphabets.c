#include < stdio.h >
/**
*main - prints alphabets
*Return: 0
*Description - All your code should be in the main function
*/
int main(void)
{
	char c;

	c = 'a';
	while 
		(c <= 'z'; ) {	
			if((c != 'q' && c != 'e') && c <= 'z')
				putchar(c);
			c++
		}
	putchar('\n');
	return (0);
}
